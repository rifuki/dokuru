{
  "name": "dokuru-landing",
  "version": "0.1.0",
  "private": true,
  "license": "Elastic-2.0",
  "type": "module",
  "scripts": {
    "check": "cargo check && cargo fmt --check && cargo clippy -- -W clippy::pedantic -W clippy::nursery -D warnings && cargo build && bun lint && bun run build",
    "lint": "cargo fmt --check && cargo clippy -- -W clippy::pedantic -W clippy::nursery -D warnings",
    "css": "tailwindcss -i style/main.css -o style/app.generated.css --minify",
    "css:dev": "tailwindcss -i style/main.css -o style/app.generated.css",
    "css:watch": "tailwindcss -i style/main.css -o style/app.generated.css --watch",
    "build": "bun run css && trunk build --release",
    "dev": "bun run css:dev && concurrently -k -n tailwind,trunk -c cyan,magenta \"bun run css:watch\" \"trunk serve\""
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.1",
    "concurrently": "^9.2.1",
    "tailwindcss": "^4.2.1"
  }
}
